public class Main
  {
  public static void main(Strings args[])
    {
    String s="priyanka";
    String result="";
    for(int i=s.length-1;i>=0;i--)
      {
      result+=str.charAt(i);
     }
     System.out.println(result);
    }
